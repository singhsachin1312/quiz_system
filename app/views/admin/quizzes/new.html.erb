<%= form_with model: [:admin, @quiz], local: true do |f| %>
  <%= f.label :title, "Quiz Title" %><br>
  <%= f.text_field :title, required: true %>
  <div>
    <%= f.label :published, "Publish Quiz?" %><br>
    <%= f.check_box :published %>
  </div>
    <%= f.submit 'Create Quiz' %>
<% end %>